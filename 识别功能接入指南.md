# 识别功能接入指南

本文档说明当前「真实 OCR」的接入方式（基于本地 MinerU，见第七节），以及历史 LangChain 模拟方案（第一~六节，仅作背景参考，当前已由 MinerU OCR 替代）。

## 一、当前现状

当前识别为**真实 OCR**，核心逻辑在 `ocr_service.py`（本地 MinerU），路由位于 `app/routers/`：

| 位置 | 说明 |
| ---- | ---- |
| `ocr_service.py` 的 `recognize_document()` | 真实 OCR：预处理 → MinerU 解析 → 结构化输出（pages / structured_text / markdown / recognized_fields） |
| `POST /api/recognize`（`app/routers/recognize.py`） | 上传识别接口（返回 `recognized_fields`） |
| `POST /api/files/upload`（`app/routers/files.py`） | 上传文件时，核心法定文书自动触发后台 OCR，结果回写 `case_file`（ocr_text/ai_result）与 `ai_parse_task` |
| `GET /api/files/{doc_type}/{file_id}/ocr` | 查询 OCR 识别状态（pending / running / done / failed / not_found） |

> 历史 LangChain 模拟方案（`_SAMPLE_RECOGNIZED` 常量）已随重构移除，不再存在。

## 二、输入输出协议（替换时必须遵守）

### 输入
- 文件落盘路径 `file_path`（`static/uploads/YYYY/MM/uuid.ext`）
- 文件 MIME 类型 `mime_type`
- 文件原始名 `file_name`

### 输出
必须返回与 `ocr_service.FIELD_NAMES`（新 `LawsuitCase` 手动录入字段）一致的字段（键名精确匹配）：

```python
{"court_case_no": "案号", "document_type": "文书类型", "plaintiff": "原告（申请人）",
 "defendant": "被告（被申请人）", "involved_parties": "涉及主体", "involved_clients": "涉及客户",
 "case_type_name": "业务类型", "standard_cause_name": "标准案由", "court_name": "受理法院/仲裁委",
 "current_status": "案件状态", "assigned_contact": "对接人", "case_description": "案由描述",
 "claim_amount": "涉案金额", "deadline_date": "关键日期", "remark": "备注"}
```

> 字段名与 `ocr_service.FIELD_NAMES` 一致，前端「应用到表单」通过 `OCR_TO_FORM_KEY_MAP` 对齐。

## 三、替换步骤总览

1. 安装依赖
2. 新建识别模块 `recognizer.py`
3. 修改 `main.py` 两处调用
4. 配置环境变量

### 3.1 安装依赖

方案 A（本地 OCR + LLM 结构化）：

```bash
pip install langchain-openai langchain-community pdfplumber pytesseract pillow
# macOS 还需安装 OCR 引擎：brew install tesseract
```

方案 B（视觉大模型，推荐）：

```bash
pip install langchain-openai langchain-community pillow pymupdf
```

### 3.2 新建 `recognizer.py`

#### 方案 A：本地 OCR 提取文本 + LangChain 结构化

```python
# recognizer.py
import json
import os

from langchain_core.output_parsers import JsonOutputParser
from langchain_core.prompts import PromptTemplate
from langchain_openai import ChatOpenAI

FIELD_NAMES = [
    "case_number", "case_type", "involved_parties", "delivery_time",
    "court_time", "court_location", "contact_phone", "plaintiff",
    "id_number", "defendant", "service_client", "stage",
    "client_contact", "processing_status", "handler", "defense_method",
    "is_closed", "judgment_result", "compensation_amount",
]

TEMPLATE = """你是一名法律文书信息提取助手。
请从以下法律文书中提取案件字段，严格返回 JSON 对象，只包含这些键：
{field_names}

文书内容：
{text}

要求：无法确定的字段返回 null；is_closed 返回 true/false；compensation_amount 返回数字。"""


def _extract_text(file_path: str, mime_type: str) -> str:
    """从 PDF 或图片中提取文本。"""
    if mime_type == "application/pdf":
        import pdfplumber
        with pdfplumber.open(file_path) as pdf:
            return "\n".join(page.extract_text() or "" for page in pdf.pages)
    import pytesseract
    from PIL import Image
    return pytesseract.image_to_string(Image.open(file_path), lang="chi_sim")


def recognize_document(file_path: str, mime_type: str) -> dict:
    """识别文件，返回 19 个案件字段。"""
    api_key = os.getenv("LLM_API_KEY")
    if api_key is None:
        raise RuntimeError("未配置 LLM_API_KEY")
    text = _extract_text(file_path, mime_type)
    prompt = PromptTemplate.from_template(TEMPLATE)
    llm = ChatOpenAI(model=os.getenv("LLM_MODEL", "gpt-4o-mini"), api_key=api_key)
    chain = prompt | llm | JsonOutputParser()
    fields = chain.invoke({
        "text": text,
        "field_names": json.dumps(FIELD_NAMES, ensure_ascii=False),
    })
    return {k: fields.get(k) for k in FIELD_NAMES}
```

#### 方案 B：视觉大模型（直接理解文书，推荐）

```python
# recognizer.py（方案 B）
import base64
import json
import os

from langchain_openai import ChatOpenAI

FIELD_NAMES = [
    "case_number", "case_type", "involved_parties", "delivery_time",
    "court_time", "court_location", "contact_phone", "plaintiff",
    "id_number", "defendant", "service_client", "stage",
    "client_contact", "processing_status", "handler", "defense_method",
    "is_closed", "judgment_result", "compensation_amount",
]

SYSTEM_PROMPT = "你是法律文书信息提取助手，根据图片内容返回 JSON，键严格为："


def _file_to_base64(file_path: str) -> str:
    with open(file_path, "rb") as f:
        return base64.b64encode(f.read()).decode("utf-8")


def recognize_document(file_path: str, mime_type: str) -> dict:
    """识别文件，返回 19 个案件字段（多模态调用）。"""
    api_key = os.getenv("LLM_API_KEY")
    if api_key is None:
        raise RuntimeError("未配置 LLM_API_KEY")
    llm = ChatOpenAI(model=os.getenv("LLM_MODEL", "gpt-4o"), api_key=api_key)
    image_b64 = _file_to_base64(file_path)
    field_names = json.dumps(FIELD_NAMES, ensure_ascii=False)
    # 多模态消息格式：根据所选模型（OpenAI / 通义 / 其它）调整
    resp = llm.invoke([image_b64, field_names])  # 示意，实际按模型文档构造
    fields = parse_fields(resp)  # 从模型输出解析 JSON（自行实现容错解析）
    return {k: fields.get(k) for k in FIELD_NAMES}
```

> 方案 B 若输入为 PDF，需先转成图片再走多模态，例如用 pymupdf：
> `fitz.open(path)` 逐页 `page.get_pixmap()` 渲染为 PNG，再调用识别。

### 3.3 修改 `main.py`（两处）

**位置 1：`POST /api/recognize`**

```python
import recognizer  # 文件顶部导入

# 原代码：
#     return {
#         "file_name": filename,
#         "file_type": ext,
#         "recognized_fields": dict(_SAMPLE_RECOGNIZED),
#     }
# 改为：
    try:
        recognized = recognizer.recognize_document(saved_path, file.content_type)
    except Exception as e:
        recognized = {}  # 识别失败不中断接口
        print("识别失败:", e)
    return {
        "file_name": filename,
        "file_type": ext,
        "recognized_fields": recognized,
    }
```

> 注意：`/api/recognize` 当前不落盘，接入真实识别时需先把上传内容写入临时文件再识别。

**位置 2：`POST /api/files/upload`（核心文书分支）**

```python
# 原代码：
#     recognized = dict(_SAMPLE_RECOGNIZED)
#     record.extracted_fields = json.dumps(recognized, ensure_ascii=False)
# 改为：
    if cfg["recognize"]:
        try:
            recognized = recognizer.recognize_document(file_path, record.mime_type)
        except Exception as e:
            recognized = {}
            print("识别失败:", e)
        record.extracted_fields = json.dumps(recognized, ensure_ascii=False)
```

> `/api/files/upload` 中 `file_path` 已是磁盘绝对路径，可直接传入识别函数。

### 3.4 配置环境变量

```bash
export LLM_API_KEY=你的大模型API Key
export LLM_MODEL=gpt-4o-mini      # 方案A；方案B 用 gpt-4o / qwen-vl-max 等
```

## 四、容错与字段对齐要求

1. **字段键名必须与 `schemas.CaseBase` 完全一致**（19 个键），否则前端表单预填会漏字段。
2. **LLM 输出校验**：`recognize_document` 返回前用 `FIELD_NAMES` 对齐（缺失键补 null）。
3. **识别失败不影响上传**：`upload_file` 中识别异常捕获后置 `recognized = {}`，文件仍正常入库。
4. **识别耗时**：LLM 调用可能数秒，建议 `recognize_file` 先落盘临时目录再识别；后续可改后台任务。
5. **日志**：用 `print` 或 `logging` 记录识别失败原因，便于排查。

## 五、验证方法

```bash
# 1. 上传核心文书（真实 PDF/图片），确认返回 recognized_fields 有内容
curl -F 'file=@/tmp/demo_data/起诉状.pdf;type=application/pdf' -F 'doc_type=core_document' http://127.0.0.1:8000/api/files/upload

# 2. 单独调用识别接口
curl -F 'file=@/tmp/demo_data/起诉状.pdf;type=application/pdf' http://127.0.0.1:8000/api/recognize

# 3. 检查返回的 19 个字段与文书内容是否一致
# 4. 前端验证：识别 → 应用到表单 → 保存 → 案件入库
```

## 六、回退方案

若 MinerU 服务不可用，`ocr_service.recognize_document()` 内部会捕获异常并返回
`ok=False, message="OCR识别失败，建议手动录入"`，上传/列表/预览等主流程不受影响。
前端「应用到表单」仅在识别成功（`ok=True`）时启用，识别失败可手动录入。

## 七、MinerU OCR 接入（ocr_service.py，当前生效）

项目已接入基于本地 MinerU 的真实 OCR 模块 `ocr_service.py`，替代上述模拟识别（`_SAMPLE_RECOGNIZED` 仅保留作回退）。

### 7.1 依赖

```bash
pip install requests pillow opencv-python-headless numpy pymupdf
```

### 7.2 功能特性

1. **上传后自动触发**：`POST /api/files/upload` 对 `core_document` 类型在保存成功后自动调用 `ocr_service.recognize_document()`，结果存入 `core_documents.extracted_fields`，并在响应中返回 `ocr_result`。
2. **OCR 前预处理**（`preprocess_image`）：灰度 → 限幅 → 降噪（双边滤波）→ 倾斜校正（minAreaRect 角度检测）→ 对比度增强（CLAHE）→ 自适应二值化（Otsu，仅低对比度/扫描件执行）。
3. **自适应文档类型**：
   - 文字型 PDF（pypdf 可提取文本）：无损拆分为逐页 PDF 提交 MinerU —— 文本/表格提取精度最高；
   - 扫描件 PDF / 图片：渲染（300 DPI）→ 预处理 → PNG 无损提交。
4. **结构化输出（含页码标记）**：每页独立 `md_content` + `middle_json`，输出 `pages / structured_text / markdown`，其中 `structured_text` 与 `markdown` 均带 `【第 N 页】` / `## ===== 第 N 页 =====` 标记。
5. **失败提示**：识别失败时统一返回 `message = "OCR识别失败，建议手动录入"`，前端 `showRecognitionResult` 会展示该提示。
6. **关键字段回填**：`recognized_fields` 通过正则从 OCR 文本提取案号/案由/原告/被告/金额等，供前端"应用到表单"。

### 7.3 调用示例

```python
from ocr_service import recognize_document
result = recognize_document("/path/起诉状.pdf", "application/pdf")
# result["ok"] True/False
# result["message"] 成功说明 / "OCR识别失败，建议手动录入"
# result["page_count"] 页数
# result["pages"] [{page, blocks, markdown}, ...]
# result["structured_text"] 带页码标记的纯文本
# result["markdown"] 带页码标题的 Markdown
# result["recognized_fields"] 关键字段 dict（19 键对齐 CaseBase）
```

### 7.4 环境变量

| 变量 | 默认值 | 说明 |
| ---- | ---- | ---- |
| `MINERU_API_URL` | `http://localhost:30000` | MinerU 服务地址 |
| `OCR_PARSE_TIMEOUT` | `600` | 单批解析超时(秒) |
| `OCR_BATCH_PAGES` | `10` | 每批提交页数 |
| `OCR_PREPROCESS_DPI` | `300` | PDF 渲染 DPI |
| `OCR_MAX_IMAGE_SIDE` | `3500` | 图像长边上限(px) |
| `OCR_BINARIZE` | `auto` | 二值化策略 auto/true/false |
| `OCR_TEXT_MIN_CHARS` | `30` | PDF 内嵌文本判定阈值 |

### 7.5 已知要点

- 页图像必须用 **PNG 无损**格式提交（JPEG 压缩伪影会严重降低 OCR 精度）。
- **OCR 异步执行**：`POST /api/files/upload` 立即返回（`ocr_result.status = "pending"`），后台线程识别完成后回写 `core_documents.extracted_fields`。
- 查询识别状态：`GET /api/files/{doc_type}/{file_id}/ocr`，返回 `status`（pending / running / done / failed / not_found）与 `ocr_result`。服务重启后内存任务丢失，接口会回读数据库兜底。
- `upload_file` 使用本地存储（`static/uploads/YYYY/MM/`），上传后后台任务直接读取本地文件识别。
- 损坏/加密文件在 `upload_file` 校验阶段即被拦截（不会进入 OCR）。

