# 阿里云 OSS 生命周期规则配置指南

本项目文件长期归档方案：上传文件按 `uploads/YYYY/MM/` 分目录存入 OSS，
通过生命周期规则自动冷热分层，控制长期存储成本。

## 前置条件
- 已创建 OSS Bucket
- 已配置环境变量：OSS_ENDPOINT / OSS_ACCESS_KEY_ID / OSS_ACCESS_KEY_SECRET / OSS_BUCKET

## 配置步骤（阿里云控制台）

### 1. 开启版本控制（可选，推荐）
Bucket → 数据安全 → 版本控制 → 开启
用途：防误删，可恢复任意历史版本

### 2. 创建生命周期规则
Bucket → 基础设置 → 生命周期 → 创建规则

#### 规则 1：文件转低频访问
- 规则名称：case-files-IA
- 匹配范围：指定前缀 → `uploads/`
- 条件：文件最后修改时间 > 90 天
- 操作：转储为「低频访问（IA）」

#### 规则 2：文件转归档存储
- 规则名称：case-files-Archive
- 匹配范围：指定前缀 → `uploads/`
- 条件：文件最后修改时间 > 180 天
- 操作：转储为「归档存储（Archive）」

#### 规则 3（可选）：超期清理
- 规则名称：case-files-expire
- 匹配范围：指定前缀 → `uploads/`
- 条件：过期天数 3650 天（10 年归档期，可按需调整）
- 操作：删除对象

## 注意事项
1. 生命周期规则对已有对象同样生效（按最后修改时间计算）
2. 归档存储读取前需「解冻」（Restore）；本系统预览接口会自动从 OSS 拉取并回填本地缓存
3. 低频 / 归档比标准存储节省约 60% / 90% 成本，适合长期归档
4. 版本控制与生命周期规则可同时使用（建议保留 1-2 个版本）

## 存储结构约定
- object key 格式：uploads/YYYY/MM/uuid.ext
- 数据库 file_path 字段与 object key 一致
- 本地 static/uploads/ 为预览缓存（首次预览从 OSS 拉取后缓存）
