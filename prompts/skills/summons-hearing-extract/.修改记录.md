- 删除***业务类型***字段（不存在）

已完成（2026-08-13）：
1. description：8个字段 → 7个字段，列表中移除"业务类型"
2. 硬性约束：8 → 7
3. 第6节"业务类型 + 标准案由（联动提取）" → "标准案由（关键词分类）"，移除映射表中"业务类型"列、移除"映射表反查"逻辑
4. 输出格式JSON模板：移除 `"业务类型"` 行
5. 提取说明模板：移除 `"业务类型"` 行
6. 第8节"应到地点"重编号为第7节

- ***案由*** 从映射表提取修改为直接提取

已完成（2026-08-13）：
1. 第6节"标准案由（关键词分类）" -> "标准案由（直接提取）"，删除案由映射表、匹配规则、未命中处理等全部关键词分类逻辑
2. 新增直接提取规则：搜索"案由"标签 -> 跳过分隔符 -> 提取至下一字段标签/空行/文末终止
3. 新增清理规则：去除前后空白、合并PDF排版换行
4. 提取说明模板：`"<已提取:命中关键词XXX | 未匹配到已知案由>"` -> `"<已提取 | 文书未包含案由信息>"`
5. 示例1-3 提取说明同步更新

- 删除***涉及主体***字段

已完成（2026-08-13）：
1. description：7个字段 -> 6个字段，列表中移除"涉及主体"
2. 硬性约束：7 -> 6
3. 删除第3节"涉及主体（NER 实体识别）"全部内容
4. 第4-7节重编号为第3-6节（受理法院/仲裁委、应到时间、标准案由、应到地点）
5. 输出格式JSON模板及提取说明模板：移除 `"涉及主体"` 行
6. 示例1-3：移除 `"涉及主体"` 字段及提取说明

- 恢复***涉及主体***字段

已完成（2026-08-13）：
1. description：6个字段 -> 7个字段，重新加入"涉及主体"
2. 硬性约束：6 -> 7
3. 恢复第3节"涉及主体（NER 实体识别）"全部内容
4. 第3-6节重编号为第4-7节（受理法院/仲裁委、应到时间、标准案由、应到地点）
5. 输出格式JSON模板及提取说明模板：新增 `"涉及主体"` 行
6. 示例1-3：新增 `"涉及主体"` 字段及提取说明

- 主体提取改为***备选名单内提取***

已完成（2026-08-13）：
1. 新增第0节"备选主体名单（提取范围硬约束）"：13家备选公司
2. 被告/被申请人、涉及主体：仅从名单中提取，名单外自然人/法人一律剔除
3. 删除自然人类别提取规则，仅保留法人/组织类别
4. 新增候选名单过滤规则（全称匹配 / 简称还原为全称）
5. 示例1-3：主体值全部替换为名单内公司

- 硬性匹配***仅限涉及主体字段***

已完成（2026-08-13）：
1. 第0节改为"涉及主体备选名单（硬性匹配）"：仅"涉及主体"字段受名单约束
2. 被告/被申请人：恢复自由 NER 提取（自然人+法人），移除候选名单过滤
3. 示例1-3：被告/被申请人恢复自然人示例，涉及主体保持名单内公司

- 新增***业务类型***字段（映射表推理）

已完成（2026-08-13）：
1. description：7个字段 -> 8个字段，新增"业务类型"
2. 硬性约束：7 -> 8
3. 新增第7节"业务类型（映射表推理）"：依据"标准案由 -> 业务类型"映射表反查；未命中输出"无法根据已有信息推断"
4. 第7节"应到地点"重编号为第8节
5. 输出格式JSON模板及提取说明模板：新增 `"业务类型"` 行
6. 示例1-3：新增 `"业务类型"` 值；示例3（案由未命中）输出"无法根据已有信息推断"

- 标准案由***改为案由映射表匹配***

已完成（2026-08-13）：
1. 第6节"标准案由（直接提取）" -> "标准案由（案由映射表匹配）"
2. 新增案由映射表（14个标准案由 + 识别关键词）及匹配规则（单案由/多案由/平局/特殊规则）
3. 删除直接提取规则（"案由"标签扫描、清理规则等）
4. 提取说明模板及示例3：`"文书未包含案由信息"` -> `"未匹配到已知案由"`

- skills 命名改为***英文***

已完成（2026-08-13）：
1. 目录名改为英文：`传票｜开庭通知书提取` -> `summons-hearing-extract`
2. SKILL.md frontmatter 的 `name` 改为英文（符合 Agent Skills 规范，小写字母+连字符）：`传票/开庭通知书提取` -> `summons-hearing-extract`
3. 正文提取规则、字段、示例均不变
- 案由映射表新增***劳动争议***类型

已完成（2026-08-13）：
1. 案由映射表新增"劳动争议"：业务类型=劳动仲裁，识别关键词=劳动争议
2. 仅当文书中识别到关键词"劳动争议"时，标准案由判定为"劳动争议"

- 被告/被申请人***触发词***新增被传唤人

已完成（2026-08-13）：
1. "被告/被申请人"字段 NER 触发词新增"被传唤人"

- 新增***改期开庭通知书***解析范围

已完成（2026-08-14）：
1. 适用范围扩展为"传票/开庭通知书/改期开庭通知书"
2. 应到时间触发词新增：改期至、变更为、调整至、重新开庭
3. 改期开庭通知书特殊规则：优先提取改期后的时间（改期至/变更为/调整至后的时间）

- 标准案由***硬性提取规则***

已完成（2026-08-19）：
1. 「未命中处理」新增：案由判定只能使用映射表内关键词，禁止使用映射表之外的关键词（如"交通事故"、"借款合同"、"劳务纠纷"等）
2. 「未命中处理」新增：`标准案由` 名称**完整直接出现**在文书原文中时可直接输出，无需额外命中关键词
3. 映射关键词未命中、且案由名称未完整直接出现在原文时，`标准案由` 一律输出 `null`，禁止自行推断/概括
