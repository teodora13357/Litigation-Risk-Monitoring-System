- 金额字段***统一输出阿拉伯数字 + 汉字金额归一化***

已完成（2026-08-20）：
1. 硬性约束新增"金额一律输出为阿拉伯数字"：不得输出汉字/大写金额
2. 金额标准化：原文汉字金额（二千/贰仟/两万/一万零三百等）先归一化为数字再输出
3. 溯源侧同步：原文汉字金额归一化为数字后与提取值精确比对

- 主体提取改为***备选名单内提取***

已完成（2026-08-13）：
1. 新增第0节"备选主体名单（提取范围硬约束）"：13家备选公司
2. 原告/申请人、被告/被申请人、涉及客户、涉及主体：仅从名单中提取，名单外自然人/法人一律剔除
3. 删除自然人类别提取规则，仅保留法人/组织类别
4. 涉及客户：新增"被代理方不在备选名单内 -> 输出 null"情形
5. 示例1-3：主体值全部替换为名单内公司

- 硬性匹配***仅限涉及主体字段***

已完成（2026-08-13）：
1. 第0节改为"涉及主体备选名单（硬性匹配）"：仅"涉及主体"字段受名单约束
2. 原告/申请人、被告/被申请人：恢复自由 NER 提取（自然人+法人），移除候选名单过滤
3. 涉及客户：恢复自由上下文推理，移除候选名单过滤
4. 示例1-3：原告/被告/涉及客户恢复自然人示例，涉及主体保持名单内公司

- 业务类型***未命中改为无法判断文案***

已完成（2026-08-13）：
1. 未命中处理：`业务类型` 由输出 `null` 改为输出 `无法根据已有信息推断`
2. 新增"标准案由已确定但不在映射表中"时业务类型输出"无法根据已有信息推断"的情形
3. 输出格式JSON模板及提取说明模板：`"业务类型"` 取值补充"无法根据已有信息推断"
4. 示例3（案由未命中）：业务类型值及提取说明改为"无法根据已有信息推断"

- 标的额***提取指标补充***

已完成（2026-08-13）：
1. 第9节"标的额"新增提取指标：以"诉讼请求金额"为准
2. 识别规则改为以字段11"诉讼请求金额"结果为基准（100字符范围内匹配）
3. 多个金额取总额（求和后标准化），单个金额直接输出

- 诉讼请求金额***改为逐项单独提取后求和***

已完成（2026-08-13）：
1. 第11节"诉讼请求金额"明确：**单独提取所有"诉讼请求金额"后求和**，仅对每项诉讼请求对应金额求和，不是所有金额求和
2. 逐项定位诉讼请求触发词，每项金额独立识别、独立标准化，再求总和
3. 第9节"标的额"改为直接取字段11结果（所有诉讼请求金额总和）
4. 示例2提取说明改为体现"两项诉讼请求金额求和"

- 删除***赔偿金***与***案号***字段

已完成（2026-08-13）：
1. description：11个字段 -> 9个字段，列表中移除"案号"、"赔偿金"
2. 硬性约束：11 -> 9
3. 删除第1节"案号（正则匹配）"与第10节"赔偿金（模式匹配 + 金额提取）"
4. 第2-9节重编号为第1-8节（原告/申请人、被告/被申请人、涉及客户、涉及主体、业务类型+标准案由、受理法院/仲裁委、标的额、诉讼请求金额）
5. 受理法院/仲裁委：移除"从案号提取"优先级，改为仅全文匹配
6. 标的额：字段引用 11 -> 9
7. 输出格式JSON模板及提取说明模板：移除 `"案号"`、`"赔偿金"` 行
8. 示例1-3：移除 `"案号"`、`"赔偿金"` 字段及提取说明；示例2标题改为"侵权-多项诉讼请求"

- 删除***涉及客户***字段

已完成（2026-08-13）：
1. description：9个字段 -> 8个字段，列表中移除"涉及客户"
2. 硬性约束：9 -> 8
3. 删除第3节"涉及客户（上下文推理）"全部内容
4. 第4-9节重编号为第3-8节（涉及主体、业务类型+标准案由、受理法院/仲裁委、标的额、诉讼请求金额）
5. 第0节名单说明：移除"涉及客户"不受约束的表述
6. 输出格式JSON模板及提取说明模板：移除 `"涉及客户"` 行
7. 示例1-3：移除 `"涉及客户"` 字段及提取说明

- skills 命名改为***英文***

已完成（2026-08-13）：
1. 目录名改为英文：`起诉状｜仲裁申请书副本提取` -> `complaint-arbitration-extract`
2. SKILL.md frontmatter 的 `name` 改为英文（符合 Agent Skills 规范，小写字母+连字符）：`起诉状/仲裁申请书提取` -> `complaint-arbitration-extract`
3. 正文提取规则、字段、示例均不变

- 案由映射表新增***劳动争议***类型

已完成（2026-08-13）：
1. 案由映射表新增"劳动争议"：业务类型=劳动仲裁，识别关键词=劳动争议
2. 仅当文书中识别到关键词"劳动争议"时，标准案由判定为"劳动争议"

- 被告/被申请人***触发词***新增被传唤人

已完成（2026-08-13）：
1. "被告/被申请人"字段 NER 触发词新增"被传唤人"

- 标的额***提取优先级改为原文优先、诉讼请求金额兜底***

已完成（2026-08-18）：
1. 第7节"标的额"由"直接取诉讼请求金额总和"改为：**优先从原文提取**（触发词：标的额、标的金额、涉案金额、争议金额，触发词后100字符内匹配金额）
2. 原文未命中（或命中但未匹配到金额）时，用字段8"诉讼请求金额"结果代替
3. 提取说明模板新增取值："已提取:原文标的额XXX"、"已提取:以诉讼请求金额代替XXX"
4. 示例1、2标的额提取说明改为"已提取:原文标的额XXX"
5. 第8节"诉讼请求金额"金额标准化引用字段修正为字段7

- 重新新增***赔偿金***字段

已完成（2026-08-18）：
1. description：8个字段 -> 9个字段，列表新增"赔偿金"
2. 硬性约束：8 -> 9
3. 新增第9节"赔偿金（模式匹配 + 金额提取）"：触发词（赔偿金、赔偿款、损害赔偿、赔偿金额），触发词后100字符内匹配金额，多个金额求和，金额标准化规则同字段7
4. 输出格式JSON模板及提取说明模板：新增 `"赔偿金"` 行
5. 示例1、2新增"赔偿金"字段（示例1无赔偿金输出 null；示例2提取30000元）；示例3新增"赔偿金": null 及提取说明"未命中触发词"
6. 同步更新 ref.json 与 schemas/complaint-arbitration-extract.schema.json（赔偿金置于"诉讼请求金额"之后）

- 金额字段***禁止四舍五入/截取小数位***

已完成（2026-08-19）：
1. 硬性约束新增第8条：金额字段（`标的额`/`诉讼请求金额`/`赔偿金`）必须保留原文全部小数位——禁止四舍五入（如 `832552.665` 不得输出 `832552.67`）、禁止截断小数（不得输出 `832552`）；求和结果同样保留精确小数位，不得舍入
2. 第7节"标的额"金额标准化列表新增"禁止四舍五入/截取小数位"细则，第8节"诉讼请求金额"、第9节"赔偿金"通过"金额标准化规则同字段7"继承该约束

- 非机动车交通事故***关键词恢复***

已完成（2026-08-19）：
1. 案由映射表"非机动车交通事故责任纠纷"识别关键词由"电动自行车"恢复为"电动车交通事故"（与其他 skill 保持一致，属误改回退）

- 示例***赔偿金字段补齐***

已完成（2026-08-19）：
1. 示例1-3 补回 `"赔偿金"` 字段及提取说明（2026-08-18"重新新增赔偿金字段"条目已声明但示例遗漏）：
   - 示例1：赔偿金 null，提取说明"未命中触发词"
   - 示例2：赔偿金 30000，提取说明"已提取:原始文本30000元"
   - 示例3：赔偿金 null，提取说明"未命中触发词"
2. 示例1标的额提取说明修正为"已提取:原文标的额50000元"（与 2026-08-18 条目一致）

- 标准案由***硬性提取规则***

已完成（2026-08-19）：
1. 「未命中处理」新增：案由判定只能使用映射表内关键词，禁止使用映射表之外的关键词（如"交通事故"、"借款合同"、"劳务纠纷"等）
2. 「未命中处理」新增：`标准案由` 名称**完整直接出现**在文书原文中时可直接输出，无需额外命中关键词
3. 映射关键词未命中、且案由名称未完整直接出现在原文时，`标准案由` 一律输出 `null`，禁止自行推断/概括

- 字段编号引用***修正自引用/错位***

已完成（2026-08-21）：
1. 第2节"被告/被申请人"：`提取方式`、`实体识别规则`、`角色排除`、`简称与全称`、`冲突处理` 由"同字段 2"（自引用）修正为"同字段 1"（原告/申请人）
2. 第3节"涉及主体"：`原告/申请人（复用字段 2 结果）` 修正为 `复用字段 1 结果`；`被告/被申请人（复用字段 3 结果）` 修正为 `复用字段 2 结果`
