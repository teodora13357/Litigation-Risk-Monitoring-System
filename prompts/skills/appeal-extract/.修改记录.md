- 金额字段***统一输出阿拉伯数字 + 汉字金额归一化***

已完成（2026-08-20）：
1. 硬性约束新增"金额一律输出为阿拉伯数字"：不得输出汉字/大写金额
2. 金额标准化：原文汉字金额（二千/贰仟/两万/一万零三百等）先归一化为数字再输出
3. 溯源侧同步：原文汉字金额归一化为数字后与提取值精确比对

- 删除***赔偿金***字段

已完成（2026-08-18）：
1. 删除第9节"赔偿金（模式匹配 + 金额提取）"全部内容
2. 字段数：11 -> 10；description 与硬性约束同步更新
3. 重编号：原第10节"诉讼请求金额"-> 第9节（金额标准化引用修正为字段8），原第11节"上诉请求"-> 第10节
4. 输出格式 JSON 模板、提取说明模板：移除 `"赔偿金"` 行
5. 示例1、2：移除 `"赔偿金"` 字段及提取说明
6. ref.json、schemas/appeal-extract.schema.json：required 与 properties 移除 `"赔偿金"`

- 金额字段***禁止四舍五入/截取小数位***

已完成（2026-08-19）：
1. 硬性约束新增第8条：金额字段（`标的额`/`诉讼请求金额`）必须保留原文全部小数位——禁止四舍五入（如 `832552.665` 不得输出 `832552.67`）、禁止截断小数（不得输出 `832552`）；求和结果同样保留精确小数位，不得舍入
2. 第8节"标的额"金额标准化列表新增"禁止四舍五入/截取小数位"细则，第9节"诉讼请求金额"通过"金额标准化规则同字段8"继承该约束

- 标的额***输出模板提取说明修正***

已完成（2026-08-19）：
1. 输出格式提取说明模板：`标的额` 移除"已提取:原文标的额XXX | 已提取:以诉讼请求金额代替XXX"（第8节无"以诉讼请求金额代替"兜底规则），改为"已提取:原始文本XXX | 未命中触发词 | 触发词命中但未匹配到金额"，与 ref.json 一致

- 涉及主体***备选名单（硬性匹配）***

已完成（2026-08-19）：
1. 新增第 0 节「涉及主体备选名单（硬性匹配）」：13 家备选主体名单，与其他提取 skill 保持一致
2. `涉及主体` 字段**只能**从名单中提取（全称或简称还原为名单全称），名单之外的自然人/法人一律不提取、输出 `null`
3. 第 4 节「涉及主体」：提取方式改为「聚合所有具名当事人（仅限备选主体名单内）」，新增「候选名单过滤（硬性）」与去重规则
4. 输出示例 2：`涉及主体` 移除名单外实体"王五"，仅保留名单内主体，提取说明同步改"已提取1个"

- 标准案由***硬性提取规则***

已完成（2026-08-19）：
1. 「未命中处理」新增：案由判定只能使用映射表内关键词，禁止使用映射表之外的关键词（如"交通事故"、"借款合同"、"劳务纠纷"等）
2. 「未命中处理」新增：`标准案由` 名称**完整直接出现**在文书原文中时可直接输出，无需额外命中关键词
3. 映射关键词未命中、且案由名称未完整直接出现在原文时，`标准案由` 一律输出 `null`，禁止自行推断/概括
