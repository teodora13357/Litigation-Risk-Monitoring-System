- 删除***赔偿金***字段

已完成（2026-08-18）：
1. 删除第9节"赔偿金（模式匹配 + 金额提取）"全部内容
2. 字段数：11 -> 10；description 与硬性约束同步更新
3. 重编号：原第10节"诉讼请求金额"-> 第9节（金额标准化引用修正为字段8），原第11节"上诉请求"-> 第10节
4. 输出格式 JSON 模板、提取说明模板：移除 `"赔偿金"` 行
5. 示例1、2：移除 `"赔偿金"` 字段及提取说明
6. ref.json、schemas/appeal-extract.schema.json：required 与 properties 移除 `"赔偿金"`

- 金额字段***禁止四舍五入/截取小数位***

已完成（2026-08-19）：
1. 硬性约束新增第8条：金额字段（`标的额`/`诉讼请求金额`）必须保留原文全部小数位——禁止四舍五入（如 `832552.665` 不得输出 `832552.67`）、禁止截断小数（不得输出 `832552`）；求和结果同样保留精确小数位，不得舍入
2. 第8节"标的额"金额标准化列表新增"禁止四舍五入/截取小数位"细则，第9节"诉讼请求金额"通过"金额标准化规则同字段8"继承该约束

- 标的额***输出模板提取说明修正***

已完成（2026-08-19）：
1. 输出格式提取说明模板：`标的额` 移除"已提取:原文标的额XXX | 已提取:以诉讼请求金额代替XXX"（第8节无"以诉讼请求金额代替"兜底规则），改为"已提取:原始文本XXX | 未命中触发词 | 触发词命中但未匹配到金额"，与 ref.json 一致
