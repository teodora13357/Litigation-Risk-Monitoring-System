- 去除举证期限

已完成（2026-08-13）：
1. description：5个字段 -> 4个字段，列表中移除"举证期限"
2. 硬性约束：5 -> 4
3. 删除第5节"举证期限（模式匹配 + 日期提取）"全部内容
4. 输出格式JSON模板：移除 `"举证期限"` 行及对应提取说明行
5. 示例1-3：移除 `"举证期限"` 字段及提取说明，重写示例标题

- 删除***涉及主体***字段

已完成（2026-08-13）：
1. description：4个字段 -> 3个字段，列表中移除"涉及主体"
2. 硬性约束：4 -> 3
3. 删除第3节"涉及主体（NER 实体识别）"全部内容
4. 第4节"受理法院/仲裁委"重编号为第3节
5. 输出格式JSON模板及提取说明模板：移除 `"涉及主体"` 行
6. 示例1-3：移除 `"涉及主体"` 字段及提取说明

- 还原***标准案由***与***举证期限***字段

已完成（2026-08-13）：
1. description：3个字段 -> 5个字段，新增"标准案由"、"举证期限"
2. 硬性约束：3 -> 5
3. 新增第4节"标准案由（直接提取）"：搜索"案由"标签，或从"本院已受理…纠纷一案"语句中提取；未命中输出 null
4. 新增第5节"举证期限（模式匹配 + 日期/期限提取）"：支持具体日期（-> YYYY-MM-DD）与相对期限（-> N日内，如 `7日内`、`十五日内`）
5. 输出格式JSON模板及提取说明模板：新增 `"标准案由"`、`"举证期限"` 行
6. 重写示例1-3：分别覆盖具体日期、相对期限、双字段缺失场景

- 恢复***涉及主体***字段

已完成（2026-08-13）：
1. description：5个字段 -> 6个字段，重新加入"涉及主体"
2. 硬性约束：5 -> 6
3. 恢复第3节"涉及主体（NER 实体识别）"全部内容
4. 第3-5节重编号为第4-6节（受理法院/仲裁委、标准案由、举证期限）
5. 输出格式JSON模板及提取说明模板：新增 `"涉及主体"` 行
6. 示例1-3：新增 `"涉及主体"` 字段及提取说明

- 主体提取改为***备选名单内提取***

已完成（2026-08-13）：
1. 新增第0节"备选主体名单（提取范围硬约束）"：13家备选公司
2. 被告/被申请人、涉及主体：仅从名单中提取，名单外自然人/法人一律剔除
3. 删除自然人类别提取规则，仅保留法人/组织类别
4. 新增候选名单过滤规则（全称匹配 / 简称还原为全称）
5. 示例1-3：主体值全部替换为名单内公司

- 硬性匹配***仅限涉及主体字段***

已完成（2026-08-13）：
1. 第0节改为"涉及主体备选名单（硬性匹配）"：仅"涉及主体"字段受名单约束
2. 被告/被申请人：恢复自由 NER 提取（自然人+法人），移除候选名单过滤
3. 示例1-3：被告/被申请人恢复自然人示例，涉及主体保持名单内公司

- 新增***业务类型***字段（映射表推理）

已完成（2026-08-13）：
1. description：6个字段 -> 7个字段，新增"业务类型"
2. 硬性约束：6 -> 7
3. 新增第6节"业务类型（映射表推理）"：依据"标准案由 -> 业务类型"映射表反查；未命中输出"无法根据已有信息推断"
4. 第6节"举证期限"重编号为第7节
5. 输出格式JSON模板及提取说明模板：新增 `"业务类型"` 行
6. 示例1-3：新增 `"业务类型"` 值；示例3（案由未命中）输出"无法根据已有信息推断"

- 标准案由***改为案由映射表匹配***

已完成（2026-08-13）：
1. 第5节"标准案由（直接提取）" -> "标准案由（案由映射表匹配）"
2. 新增案由映射表（14个标准案由 + 识别关键词）及匹配规则（单案由/多案由/平局/特殊规则）
3. 删除直接提取规则（"案由"标签扫描、受理语句提取、清理规则等）
4. 提取说明模板及示例3：`"文书未包含案由信息"` -> `"未匹配到已知案由"`

- skills 命名改为***英文***

已完成（2026-08-13）：
1. 目录名改为英文：`举证通知书提取` -> `evidence-notice-extract`
2. SKILL.md frontmatter 的 `name` 改为英文（符合 Agent Skills 规范，小写字母+连字符）：`举证通知书提取` -> `evidence-notice-extract`
3. 正文提取规则、字段、示例均不变
- 案由映射表新增***劳动争议***类型

已完成（2026-08-13）：
1. 案由映射表新增"劳动争议"：业务类型=劳动仲裁，识别关键词=劳动争议
2. 仅当文书中识别到关键词"劳动争议"时，标准案由判定为"劳动争议"

- 被告/被申请人***触发词***新增被传唤人

已完成（2026-08-13）：
1. "被告/被申请人"字段 NER 触发词新增"被传唤人"

- 被告/被申请人***新增文书开头兜底识别***

已完成（2026-08-18）：
1. 第2节"被告/被申请人（NER 实体识别）"新增"文书开头兜底识别（触发词未命中时）"规则
2. 全文未命中 `被告`、`被申请人` 等触发词时，从文书开头（抬头 `*公司（单位/你单位）：`，通常前 1–3 行）识别以组织后缀结尾的实体作为被告/被申请人
3. 简称还原为全称；开头出现多个实体时结合上下文排除原告方
4. 仅在 SKILL.md 第 2 节内修改，输出模板与示例保持不变
