- 判决/裁定/调解 skill 适用范围***新增仲裁裁决书***

已完成（2026-08-21）：
1. 适用范围由"判决书/裁定书/调解书"扩展为"判决书/裁定书/调解书/仲裁裁决书"，避免仲裁裁决书被误判为不可解析
2. 案号提取新增仲裁案号兜底正则：`[\u4e00-\u9fa5]{2,}仲案字[（(]\d{4}[）)]第\d+号`，支持 `太劳人仲案字（2026）第952号` 格式
3. description 同步更新为包含"仲裁裁决书"
4. 诉讼请求金额新增"撤回/变更不影响求和范围"规则：即使庭审中原告撤回部分诉求，仍按起诉状/申请书中全部诉讼请求金额求和，不得剔除
5. 赔偿金新增"裁判主文兜底"：未命中常规触发词时，从裁判主文"赔偿…金额元"中提取赔偿金
6. 裁判结果"案件受理费"按文书类型区别处理：调解书/仲裁裁决书保留主文后紧接的案件受理费句；判决书/裁定书止于最后一项裁判项，不包含案件受理费句

- 金额字段***统一输出阿拉伯数字 + 汉字金额归一化***

已完成（2026-08-20）：
1. 硬性约束新增"金额一律输出为阿拉伯数字"：不得输出汉字/大写金额
2. 金额标准化：原文汉字金额（二千/贰仟/两万/一万零三百等）先归一化为数字再输出
3. 溯源侧同步：原文汉字金额归一化为数字后与提取值精确比对

- 主体提取改为***备选名单内提取***

已完成（2026-08-13）：
1. 新增第0节"备选主体名单（提取范围硬约束）"：13家备选公司
2. 原告/申请人、被告/被申请人、涉及主体：仅从名单中提取，名单外自然人/法人一律剔除
3. 删除自然人类别提取规则，仅保留法人/组织类别
4. 示例1-3：主体值全部替换为名单内公司（含裁判结果文本中的主体引用）

- 硬性匹配***仅限涉及主体字段***

已完成（2026-08-13）：
1. 第0节改为"涉及主体备选名单（硬性匹配）"：仅"涉及主体"字段受名单约束
2. 原告/申请人、被告/被申请人：恢复自由 NER 提取（自然人+法人），移除候选名单过滤
3. 示例1-3：原告/被告恢复自然人示例（含裁判结果文本），涉及主体保持名单内公司

- 业务类型***未命中改为无法判断文案***

已完成（2026-08-13）：
1. 未命中处理：`业务类型` 由输出 `null` 改为输出 `无法根据已有信息推断`
2. 新增"标准案由已确定但不在映射表中"时业务类型输出"无法根据已有信息推断"的情形
3. 输出格式JSON模板及提取说明模板：`"业务类型"` 取值补充"无法根据已有信息推断"
4. 示例3（案由未命中）：业务类型值及提取说明改为"无法根据已有信息推断"

- 删除***代理人信息***字段

已完成（2026-08-13）：
1. description：12个字段 -> 11个字段，列表中移除"代理人信息"
2. 硬性约束：12 -> 11
3. 删除第8节"代理人信息（NER 实体识别 + 模式匹配）"全部内容
4. 第9-12节重编号为第8-11节（标的额、赔偿金、诉讼请求金额、裁判结果）
5. 输出格式JSON模板及提取说明模板：移除 `"代理人信息"` 行
6. 示例1-3：移除 `"代理人信息"` 字段及提取说明

- 标的额***提取指标补充***

已完成（2026-08-13）：
1. 第8节"标的额"新增提取指标：以"诉讼请求金额"为准
2. 识别规则改为以字段10"诉讼请求金额"结果为基准（100字符范围内匹配）
3. 多个金额取总额（求和后标准化），单个金额直接输出

- 诉讼请求金额***改为逐项单独提取后求和***

已完成（2026-08-13）：
1. 第10节"诉讼请求金额"明确：**单独提取所有"诉讼请求金额"后求和**，仅对每项诉讼请求对应金额求和，不是所有金额求和
2. 逐项定位诉讼请求触发词，每项金额独立识别、独立标准化，再求总和
3. 第8节"标的额"改为直接取字段10结果（所有诉讼请求金额总和）
4. 示例2提取说明改为体现"两项诉讼请求金额求和"

- skills 命名改为***英文***

已完成（2026-08-13）：
1. 目录名改为英文：`判决书｜裁定书｜调解书提取` -> `judgment-ruling-mediation-extract`
2. SKILL.md frontmatter 的 `name` 改为英文（符合 Agent Skills 规范，小写字母+连字符）：`判决书｜裁定书｜调解书提取` -> `judgment-ruling-mediation-extract`
3. 正文提取规则、字段、示例均不变

- 案由映射表新增***劳动争议***类型

已完成（2026-08-13）：
1. 案由映射表新增"劳动争议"：业务类型=劳动仲裁，识别关键词=劳动争议
2. 仅当文书中识别到关键词"劳动争议"时，标准案由判定为"劳动争议"

- 被告/被申请人***触发词***新增被传唤人

已完成（2026-08-13）：
1. "被告/被申请人"字段 NER 触发词新增"被传唤人"

- 标的额***提取优先级改为原文优先、诉讼请求金额兜底***

已完成（2026-08-18）：
1. 第8节"标的额"由"直接取诉讼请求金额总和"改为：**优先从原文提取**（触发词：标的额、标的金额、涉案金额、争议金额，触发词后100字符内匹配金额）
2. 原文未命中（或命中但未匹配到金额）时，用字段10"诉讼请求金额"结果代替
3. 提取说明模板新增取值："已提取:原文标的额XXX"、"已提取:以诉讼请求金额代替XXX"
4. 示例1、2标的额提取说明改为"已提取:原文标的额XXX"

- 金额字段***禁止四舍五入/截取小数位***

已完成（2026-08-19）：
1. 硬性约束新增第9条：金额字段（`标的额`/`赔偿金`/`诉讼请求金额`）必须保留原文全部小数位——禁止四舍五入（如 `832552.665` 不得输出 `832552.67`）、禁止截断小数（不得输出 `832552`）；求和结果同样保留精确小数位，不得舍入
2. 第8节"标的额"金额标准化列表新增"禁止四舍五入/截取小数位"细则，第9节"赔偿金"、第10节"诉讼请求金额"通过"金额标准化规则同字段8"继承该约束

- 涉及主体***其他诉讼参与人触发词恢复***

已完成（2026-08-19）：
1. "其他诉讼参与人"补充触发词 `证人`、`鉴定人`（此前被误删，与 complaint/appeal skill 保持一致）

- 标的额***输出模板提取说明修正***

已完成（2026-08-19）：
1. 输出格式提取说明模板：`标的额` 由"已提取:原始文本XXX"改为"已提取:原文标的额XXX"，与 ref.json 及示例一致

- 标准案由***硬性提取规则***

已完成（2026-08-19）：
1. 「未命中处理」新增：案由判定只能使用映射表内关键词，禁止使用映射表之外的关键词（如"交通事故"、"借款合同"、"劳务纠纷"等）
2. 「未命中处理」新增：`标准案由` 名称**完整直接出现**在文书原文中时可直接输出，无需额外命中关键词
3. 映射关键词未命中、且案由名称未完整直接出现在原文时，`标准案由` 一律输出 `null`，禁止自行推断/概括

- 字段编号引用***修正自引用***

已完成（2026-08-21）：
1. 第9节"赔偿金"：`提取方式`、`识别规则` 由"同字段 9"（自引用）修正为"同字段 8"（标的额）

- 裁判结果***主文起点触发词新增***

已完成（2026-08-21）：
1. 第11节"裁判结果"主文起点触发词新增 `决定如下`（决定书类文书）
