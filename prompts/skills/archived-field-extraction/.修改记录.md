- 金额字段触发词***语义匹配 + 同义词扩展（与活跃 skill 同步）***

已完成（2026-08-24）：
1. 金额字段（标的额/赔偿金/诉讼请求金额）触发词由封闭词表精确匹配改为「示例词表 + 语义等价匹配」：语义相同或相近、且明确指向该字段金额的表述同样视为命中
2. 标的额触发词新增：诉讼标的额、诉讼标的、标的款、标的数额、案涉金额、争议标的、诉争金额、涉诉金额、案件标的额
3. 诉讼请求金额触发词新增：诉讼请求金额、请求金额、诉请金额、请求的金额、主张金额、要求支付、要求赔偿、诉求金额、请求数额
4. 赔偿金触发词新增：赔偿费用、赔付款、赔付金额、损失赔偿、赔偿数额、赔付数额

- 诉讼请求金额***新增诉称触发词***

已完成（2026-08-24）：
1. "诉讼请求金额"识别触发词新增"诉称"

- 案由/业务类型映射***补充支付工资与侵权责任纠纷***

已完成（2026-08-24）：
1. "追索劳动报酬纠纷"识别关键词新增"支付工资"
2. 案由映射表补充"侵权责任纠纷"

- skills 命名改为***英文***

模版文件创建，包含所有字段，后续细分文件修改

已完成（2026-08-13）：
1. 目录名改为英文：`.文件字段提取` -> `.field-extraction`
2. SKILL.md frontmatter 的 `name` 改为英文（符合 Agent Skills 规范，小写字母+连字符）：`文件解析提取` -> `field-extraction`
3. 正文提取规则均不变

- 案由映射表新增***劳动争议***类型

已完成（2026-08-13）：
1. 案由映射表新增"劳动争议"：业务类型=劳动仲裁，识别关键词=劳动争议
2. 仅当文书中识别到关键词"劳动争议"时，标准案由判定为"劳动争议"

- 被告/被申请人***触发词***新增被传唤人

已完成（2026-08-13）：
1. "被告/被申请人"字段 NER 触发词新增"被传唤人"

- 字段顺序***业务类型***统一放到标准案由后面

已完成（2026-08-21）：
1. frontmatter description 字段清单顺序：业务类型、标准案由 -> 标准案由、业务类型
2. 第6+7节标题：`业务类型 + 标准案由` -> `标准案由 + 业务类型`
3. 输出格式 JSON 模板与提取说明模板：`业务类型` 行移到 `标准案由` 行之后
4. ref.json 与 schemas 同步：required 数组、properties 键顺序均按"标准案由 -> 业务类型"排列
